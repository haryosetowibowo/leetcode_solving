import numpy as np
class Solution:
    def findDuplicates(self, nums: List[int]) -> List[int]:
        unique, counts = np.unique(nums, return_counts=True)
        ans = unique[np.where(counts > 1)]
        return ans