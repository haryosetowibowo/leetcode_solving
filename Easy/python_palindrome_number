class Solution:
    def isPalindrome(self, x: int) -> bool:
        n = str(x)
        n1 = ''
        for i in range(len(n)-1,-1,-1):
            n1 = n1+n[i]
        if n1 == n:
            return True
        else:
            return False